services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - agent_data:/app/data
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - DATABASE_URL=sqlite:///./data/agent_auditor.db
    restart: unless-stopped

volumes:
  agent_data:
