# Database
DATABASE_URL=sqlite:///./agent_auditor.db

# OpenAI (optional — leave blank to use mock agent)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Rate limiting
RATE_LIMIT=10/minute

# Custom agent webhook timeout (seconds)
CUSTOM_AGENT_TIMEOUT=15
